{
  "private": true,
  "name": "lippe.dev",
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "lint": "turbo run lint"
  },
  "devDependencies": {
    "turbo": "^2.0.4"
  },
  "workspaces": [
    "apps/*",
    "packages/*",
    "config/*"
  ],
  "packageManager": "bun@1.1.15",
  "dependencies": {
    "server-only": "^0.0.1"
  }
}